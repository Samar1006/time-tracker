# time-tracker-app

Angular dashboard for the time-tracker hackathon project.

See the [root README](../../README.md) for full setup. From this directory:

```bash
npm install && npm start
```

App runs at **http://localhost:4200** (proxies `/api` → `http://127.0.0.1:4000`).
