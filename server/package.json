{
  "name": "time-tracker-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Time-tracker backend — activity ingestion, Redis storage, hour-by-hour aggregation",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "optionalDependencies": {
    "redis": "^4.7.0"
  }
}
