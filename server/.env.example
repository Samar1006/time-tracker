# Copy to .env and fill in. Never commit the real .env.

# Optional: Redis for persisting raw activity events.
# If unset/unreachable, events are stored in-memory (fine for tests/demo).
REDIS_URL=

PORT=4000
